import type { SkillTemplate } from "./types.js";

/**
 * 手工测试文档生成 skill 模板。
 * CLI 的 manual-test 命令只负责创建基础模板，完整测试步骤由该 skill 结合上下文补全。
 */
export const manualTestWorkbenchSkillTemplate: SkillTemplate = {
  name: "code-helper-manual-test-workbench",
  directoryName: "code-helper-manual-test-workbench",
  fileName: "manual-test-workbench.SKILL.md",
  content: `---
name: code-helper-manual-test-workbench
description: 当用户要求生成手工测试文档、验收清单、页面或可视化验收、真实浏览器链路验证、人工业务验收、回归测试步骤或测试补充说明时必须使用。该 skill 要从 SQLite 读取 plan、status、result、manual_test 权威正文和相关页面或代码上下文，并通过 CAS 更新手工测试正文；CLI manual-test 只生成模板，完整测试步骤必须由该 skill 结合上下文补全。
---

# Code Helper 手工测试生成

## 目标

当任务涉及页面、可视化、真实浏览器链路、人工业务验收或回归测试步骤时，生成可由用户直接执行的手工测试文档。文档不能停留在空模板或泛泛 checklist，必须结合需求、计划、当前状态、实施记录和相关页面或代码上下文，写清楚测试环境、前置数据、操作步骤、预期结果、回归范围和阻塞记录。

## 适用场景

- 用户要求生成或补全手工测试文档
- 用户要求生成验收清单、人工验收步骤、业务验收说明或回归测试步骤
- 页面、组件交互、可视化图表、浏览器跳转、登录态、权限链路或真实浏览器流程需要验证
- 视觉、文案、布局、响应式、可访问性或多端展示需要人工确认
- 自动化测试不适合覆盖，或者只能由业务人员、产品、测试或用户手工验收
- CLI 已生成 \`手工测试.md\` 模板，但仍需要 agent 根据上下文补全完整步骤

## 必读上下文

执行前必须读取并综合以下内容：

1. 用户本轮需求、验收口径和补充约束。
2. SQLite 已初始化时，使用 \`document show <任务> plan --json\` 读取计划中的验收标准、依赖关系和回归范围。
3. 使用 \`document show <任务> status --json\` 读取当前执行节点、已完成范围、阻塞点和下一步。
4. 使用 \`document show <任务> result --json\` 读取实际改动、验证结论、风险和未完成项。
5. 运行 manual-test CLI 创建模板后，必须立即使用 \`document show <任务> manual_test --json\` 读取模板正文与 revision；已有 manual_test 时同样从 SQLite 读取并保留有效内容。
6. 与测试对象相关的页面、组件、路由、命令、接口、配置、数据结构或业务规则代码。
7. 测试策略规则，尤其是页面、可视化和真实浏览器链路由用户手工执行，工具只执行纯逻辑测试。

如果缺少某个文档，不要虚构内容；在手工测试文档的阻塞记录中写明缺失项、影响范围和需要补充的信息。

如果本轮是直接执行且已经进入 \`code-helper-completion-record\` 流程，默认把必要的页面或真实浏览器验证步骤写入同一完成记录，不创建独立 \`手工测试.md\`。只有用户明确要求生成验收清单、手工测试文档或独立回归步骤时，才建立计划任务或按用户授权生成相应文档；不得仅因存在完成记录而反向补齐 plan/status/result。

## 最终产物

SQLite 权威输出为任务的 \`manual_test\` 文档；成功 CAS 更新后自动刷新兼容投影：

\`.code-helper/local/docs/result-doc/<中文功能名>/手工测试.md\`

文档必须使用中文命名、中文标题和中文总结。功能名必须尽量来自已有 plan-doc、status-doc 或 result-doc；如果只能从用户描述推断，要保留可区分后缀，避免多个任务覆盖同一目录。

## CLI 边界

\`npx @skrupellose/code-helper manual-test <中文功能名>\` 只负责生成结构化模板并写入 SQLite，同时生成兼容投影，不代表已经完成完整测试分析。命令结束后必须执行 \`document show <任务> manual_test --json\`，补全正文后通过 \`document update <任务> manual_test --body-stdin --expected-revision <N> --summary <中文摘要> --json\` 写回；不得默认直接编辑 Markdown 投影。

完整测试步骤、测试数据、预期结果、回归范围和阻塞判断，必须由本 skill 读取需求、计划、状态、实施记录和相关代码后补全。不要把 CLI 生成的空模板当作最终交付。

## 文档结构

手工测试文档必须至少包含以下小节：

1. 测试环境
2. 前置数据
3. 操作步骤
4. 预期结果
5. 回归范围
6. 阻塞记录

推荐结构：

\`\`\`markdown
# <中文功能名> 手工测试

## 测试目标

## 测试环境

## 前置数据

## 操作步骤

| 编号 | 场景 | 前置条件 | 操作步骤 | 预期结果 | 结果记录 |
|---|---|---|---|---|---|

## 回归范围

## 阻塞记录

## 验收结论
\`\`\`

## 编写规则

### 1. 测试目标

先用中文说明本轮要验收什么、哪些内容不在本轮范围内、哪些结论来自已有实施记录或状态记录。

### 2. 测试环境

必须写清：

- 测试环境名称或访问方式，例如本地、测试环境、预发环境或生产灰度
- 浏览器、设备、账号、权限、配置开关、依赖服务和版本要求
- 需要用户自行启动或打开的页面、应用、服务或浏览器环境
- 工具侧已经执行或允许执行的纯逻辑测试命令

不要要求 agent 代替用户执行页面、可视化或真实浏览器链路测试。

### 3. 前置数据

必须写清：

- 需要准备的账号、角色、权限、组织、项目、订单、记录、文件或配置
- 数据创建方式、数据状态、关键字段和值
- 可复用的现有数据和需要用户补充的数据
- 缺少数据时对测试结果的影响

### 4. 操作步骤

每个场景必须可执行、可复查。步骤要写成用户能照着操作的动作，不写“验证正常”这种不可执行描述。

每个场景至少包含：

- 场景名称
- 前置条件
- 逐步操作
- 每步或关键步骤的预期结果
- 结果记录位置

涉及页面、可视化或浏览器链路时，应覆盖：

- 首次进入和刷新
- 正常流程
- 空状态、加载中、错误态或权限不足
- 表单输入、筛选、切换、保存、取消、删除或回退
- 响应式或关键尺寸
- 文案、数值、颜色、图表、状态提示或交互反馈
- 与上下游页面、接口、任务或数据状态的联动

### 5. 预期结果

预期结果必须可判定，避免只写“符合预期”。优先写清：

- 页面上应出现或不应出现的文字、按钮、状态、数据或提示
- 数据是否新增、更新、删除、保持不变或进入指定状态
- 错误提示、权限拦截、加载状态和失败恢复方式
- 验收通过的边界条件和不通过的判定标准

### 6. 回归范围

结合 plan-doc、status-doc、result-doc 和代码影响面列出需要回归的模块。至少说明：

- 本次改动直接影响的页面、命令、接口、配置、数据结构或业务流程
- 可能被连带影响的旧功能
- 不需要回归的范围和理由
- 纯逻辑测试可由工具执行的范围，以及页面和浏览器链路必须由用户手工执行的范围

### 7. 阻塞记录

阻塞记录必须保留当前无法完成的测试项，并写清：

- 阻塞项
- 阻塞原因
- 缺少的环境、数据、权限、账号、接口、设计稿或业务确认
- 对验收结论的影响
- 解除阻塞后的复测入口

如果没有阻塞，也要写“暂无阻塞”，不能省略该小节。

## 验证边界

- 页面、可视化、组件交互、视觉、真实浏览器链路和人工业务验收由用户手工执行。
- 工具只执行纯逻辑测试，例如函数单元测试、数据转换测试、schema 校验、非浏览器集成测试或 CLI 输出测试。
- 不要为了页面验收临时启动浏览器自动化、截图比对或端到端测试，除非用户明确要求并授权。
- 自动化命令的结果只能作为补充证据，不能替代手工测试文档中的操作步骤和预期结果。

## 更新方式

如果 SQLite 中 \`manual_test\` 文档已存在：

1. 保留仍然有效的测试环境、数据和历史结论。
2. 根据最新 plan、status、result 权威正文和代码上下文更新步骤。
3. 标记过期场景，说明删除或替换原因。
4. 补充新增影响面的回归范围。
5. 更新阻塞记录和验收结论。

如果 SQLite 中 \`manual_test\` 文档不存在：

1. 先确认中文功能名和目标目录。
2. 运行 \`manual-test <中文功能名>\` 创建模板，再用 \`document show\` 读取新 revision。
3. 在内存中生成完整结构，通过带 revision 的 \`document update --body-stdin\` 写入；Markdown 路径仅作为自动投影。

## 完成标准

文档满足以下条件才算完成：

- 已读取并综合需求、plan-doc、status-doc、result-doc 和相关页面或代码上下文。
- 包含测试环境、前置数据、操作步骤、预期结果、回归范围和阻塞记录。
- 每个核心场景都有可执行步骤和可判定预期结果。
- 明确区分工具可执行的纯逻辑测试和用户必须手工执行的页面、可视化、真实浏览器链路。
- 阻塞项、缺失数据和未覆盖风险被单独记录。
- SQLite 已初始化时通过 \`document show → document update --body-stdin --expected-revision\` 完成读取与写入，Markdown 仅为自动投影。
- 文档路径为 \`.code-helper/local/docs/result-doc/<中文功能名>/手工测试.md\`。`
};
