import type { SkillTemplate } from "./types.js";

/**
 * 代码审查与修复闭环 Skill 模板。
 * Skill 使用平台中立的 agent 表述，使 Codex、Claude、Grok 和 GitHub Copilot
 * 都能遵循同一套“先只读审查、再授权修复、最后逐项复审”边界。
 */
export const reviewFixSkillTemplate: SkillTemplate = {
  name: "code-helper-review-fix",
  directoryName: "code-helper-review-fix",
  fileName: "review-fix.SKILL.md",
  content: `---
name: code-helper-review-fix
description: 当用户要求代码审查、review 最近改动、检查提交或 diff、判断主问题是否解决、列出待优化项，或在已有 findings 基础上要求“进行修复”“依次修复”“复审修复结果”时必须使用。默认先做只读、证据驱动的审查；只有用户明确授权修复后才能修改文件，并在修复后按稳定 finding ID 逐项复审。适用于 Codex、Claude、Grok、GitHub Copilot 等 agent 工具。
---

# Code Helper 代码审查与修复

## 目标

把代码审查和后续修复组织成可复查的闭环：先用证据回答主问题是否解决并列出 findings，再等待用户明确授权修复，最后逐项验证和复审。纯只读审查的 findings 默认只输出到当前对话，不修改过程文档；只有用户明确授权修复，或另行明确授权记录本次 review 后，才能把相应结论写入现有实施记录。

## 适用场景

- 用户要求 review 当前改动、最近提交、某个 diff、PR、功能实现或修复结果
- 用户要求确认“主问题是否解决”“还有哪些待优化项”
- 用户要求只读检查，不希望当前阶段修改代码
- 已经存在 review findings，用户要求“进行修复”“依次修复”或指定修复某一项
- 修复后需要复审，确认每个 finding 是否真正关闭
- 多个 agent 分别承担审查、修复和复审，需要稳定交接 finding 状态

## 平台中立原则

- 使用“主会话”“执行子代理”“审查者”“修复执行者”等通用角色描述，不依赖某个 agent 工具的专属命令。
- Codex、Claude、Grok、GitHub Copilot 或其它 agent 工具只要能读取项目文件，都应遵守相同的只读边界、授权边界和复审格式。
- 如果当前会话是主会话，按项目 Agent 协作规范把具体代码读取、修复和测试交给执行子代理；如果当前会话已被明确派发为执行子代理，则在派发范围内直接完成，不再二次转派。

## 必读上下文

审查前按任务范围读取：

1. 用户本轮指定的审查目标、基线提交、diff、文件范围和禁止事项。
2. 项目入口文档及其索引到的专题规则。
3. 活动任务对应的 plan-doc、status-doc 和 result-doc；没有活动任务时不要虚构过程文档。
4. 与主问题直接相关的源码、测试、配置、类型、调用链路和已有实现。
5. 必要的历史变更或提交差异；只读取能够支撑结论的范围，不扩张成无边界全仓扫描。

如果审查范围可以从用户指定的文件、提交或当前变更中安全确定，应直接开始只读检查；只有不同基线会实质改变结论且无法从项目状态判断时，才请求用户补充。

## 阶段一：只读审查

### 1. 固定只读边界

- 用户只要求审查、检查或评估时，不修改源码、测试、配置、过程文档或生成副本。
- 即使存在活动任务，纯只读审查的 findings 也默认只输出到当前对话；活动任务本身不构成写入实施记录或状态记录的授权。
- 可以执行与审查范围匹配的只读检查和纯逻辑测试，但不得把“测试通过”直接等同于“没有代码问题”。
- 不自动安装依赖、不启动服务、不访问外部系统、不提交、不推送、不归档、不发布。

### 2. 先回答主问题

审查结果必须先用明确结论回答：

- 主问题已解决
- 主问题部分解决
- 主问题未解决
- 证据不足，暂时无法确认

随后给出支撑结论的文件、行号、调用链或测试证据。不要先堆叠一般性建议，让用户自己推断主结论。

### 3. 生成稳定 finding

每个可执行问题使用稳定 ID：

- P0：会造成数据损坏、安全问题、发布阻断或核心功能错误
- P1：会造成明确功能缺陷、重要回归或跨平台失败
- P2：影响可维护性、边界完整性或后续稳定性，但不直接阻断当前主链路

ID 格式固定为 \`RF-P0-001\`、\`RF-P1-001\`、\`RF-P2-001\`，同一轮复审继续沿用原 ID，不因排序或修复状态变化重新编号。

每个 finding 必须包含：

| 字段 | 要求 |
|---|---|
| Finding ID | 稳定 ID，例如 RF-P1-001 |
| 优先级 | P0 / P1 / P2 |
| 标题 | 一句话描述具体问题 |
| 证据 | 绝对或项目相对文件路径、紧凑行号、实际代码或行为证据 |
| 影响 | 说明会导致什么错误、回归或维护风险 |
| 建议 | 给出最小可验证修复方向，不在只读阶段直接实施 |
| 状态 | 审查阶段固定为“待确认”或“待修复” |

没有发现问题时明确写“未发现可执行 finding”，同时说明尚未覆盖的范围和未运行的验证，不能编造低价值问题凑数。

### 4. 审查输出顺序

1. 主问题结论
2. Findings，按 P0、P1、P2 排序
3. 已验证范围
4. 未验证范围与风险
5. 建议下一步

审查结束后停止；除非用户在同一请求中已经明确授权修复，否则不得直接修改。

## 阶段二：用户授权后修复

### 1. 授权判定

只有用户明确表达以下意图时才进入修复：

- 进行修复
- 依次修复
- 修复 RF-P1-001
- 按 findings 全部处理
- 等效的明确修改授权

“看看”“review”“检查一下”“有什么问题”“给建议”都不构成修复授权。用户只授权某个 ID 时，只修复该 ID 及其不可分割的直接依赖，不顺手处理其它 finding。

### 2. 修复顺序

1. 按用户指定范围或 P0 → P1 → P2 顺序处理。
2. 每次只推进当前 finding，先确认根因、影响文件和最小验证方式。
3. 保留用户已有改动，不回滚无关内容，不扩大到未授权重构。
4. 修复后执行与风险相称的纯逻辑测试、类型检查或静态检查。
5. 页面、可视化、视觉和真实浏览器链路只补充手工测试文档，由用户执行；工具不默认运行浏览器自动化。
6. 某项无法完成时，保留原 finding ID，记录阻塞原因、恢复条件和未验证范围，再继续用户允许的下一项。

### 3. 授权后的过程记录

只有满足以下任一条件时，才能更新过程文档：

- 用户明确授权修复：可在现有活动任务中记录审查基线、授权范围、修复和复审结果。
- 用户另行明确授权记录本次 review：只记录审查结论和 findings，不因此获得任何代码修改权限。

满足记录授权且存在活动任务时，写入：

\`code-helper-docs/result-doc/<中文功能名>/实施记录.md\`

在现有实施记录中增加或更新“代码审查与修复”小节，至少记录：

- 审查范围和基线
- 主问题结论
- finding ID、优先级和初始状态
- 每项实际修改
- 验证命令和结论
- 复审状态
- 风险与未完成项

仍影响下一步决策的 finding 或阻塞同步到 \`code-helper-docs/status-doc/<中文功能名>-状态.md\`。写入范围不得超过用户授权；不要创建 \`代码审查.md\`、\`review.md\` 等未被 code-helper 支持的新结果文档类型。

没有记录授权时，findings 只保留在当前对话，不写入任何过程文档。如果当前没有活动任务，即使用户授权记录 review，也不要自行虚构 plan-doc、status-doc 或 result-doc；应先在对话中汇报，并在确实需要建立任务文档时另行取得授权。

## 阶段三：逐项复审

修复完成后必须回到原 findings 清单，按原 ID 逐项复审：

| 复审状态 | 判定 |
|---|---|
| 已关闭 | 根因已修复，相关验证通过，没有发现同一问题残留 |
| 部分关闭 | 只修复部分路径、验证不完整或仍有明确剩余风险 |
| 未关闭 | 修复无效、问题仍可复现或关键验证失败 |
| 被阻塞 | 缺少环境、权限、数据或外部条件，当前无法完成复审 |

复审不能只转述修复执行者的结论，必须重新读取实际改动和验证证据。发现新问题时创建新的 finding ID，不复用或篡改旧 ID。

最终输出必须包含：

1. 主问题的最终结论
2. 每个 finding 的最终状态
3. 实际修改文件
4. 验证命令与结果
5. 剩余风险和未完成项
6. 需要用户决定的下一步

## 与完成检查的衔接

- 纯只读 review 没有产生实现或文档变更时，不触发功能完成检查。
- 用户另行授权记录 review 并产生过程文档变更时，准备最终回复前必须使用 \`code-helper-completion-review\`。
- 授权修复产生代码、测试或过程文档变更后，准备最终回复前必须使用 \`code-helper-completion-review\`。
- 有 P0、P1 finding 仍为“未关闭”“部分关闭”或“被阻塞”时，不得把功能描述为全部完成，也不得建议直接归档。
- 更新长期记忆、归档、提交、推送或发布仍需用户明确确认，复审通过不会自动授权这些动作。

## 边界规则

- 不在只读审查阶段自动修改代码或文档。
- 不把模糊请求解释为修复授权。
- 不用测试通过代替代码审查，也不用代码审查代替必要测试。
- 不删除、降级或隐藏未关闭 finding。
- 不为追求“全部关闭”而扩大范围或修改验收口径。
- 不创建 code-helper 尚未支持的结果文档类型。
- 不自动提交、推送、归档、发布或更新长期记忆。
- 不依赖某个 agent 平台专属命令，确保不同工具得到一致工作流。`
};
