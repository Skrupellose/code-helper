import type { SkillTemplate } from "./types.js";

/** 需求探索、规格整理与澄清 Skill 模板。 */
export const requirementClarificationSkillTemplate: SkillTemplate = {
  name: "code-helper-requirement-clarification",
  directoryName: "code-helper-requirement-clarification",
  fileName: "requirement-clarification.SKILL.md",
  content: `---
name: code-helper-requirement-clarification
description: 当用户只有模糊想法、需要探索需求可行性、补齐目标与验收条件、整理需求规格或澄清进入计划前的歧义时使用。该 skill 先保留原始需求，再区分目标、非目标、用户场景、限制、验收条件和未决问题；澄清不代表授权实施，也不替代后续计划。
---

# Code Helper 需求探索与澄清

## 目标

把模糊需求整理成可审阅、可验证的中文需求规格。只使用用户确认的信息，不把推测写成事实；缺失信息转成带稳定 ID 的待澄清问题。

## 适用场景

- 用户表达了想法或问题，但目标、范围或验收条件尚不完整
- 用户要求先探索可行性、比较边界或确认是否值得进入计划
- 已有需求存在歧义，需要在拆计划前澄清
- 用户要求整理正式需求规格、用户场景或验收条件

用户已经提供完整需求并直接要求拆分多阶段执行计划时，使用计划管理能力；用户只要求代码审查时，不要把审查请求改写成新需求。

## 探索阶段

先保留用户原始描述，再整理：

1. 目标：最终要改善的可观察结果
2. 非目标：本阶段明确不处理的相邻事项
3. 用户与场景：谁在什么情况下使用或受影响
4. 限制：兼容性、时间、权限、数据、发布和外部依赖边界
5. 验收条件草案：可以通过测试或人工验收观察的结果
6. 待澄清问题：使用 Q-001、Q-002 等稳定 ID，并标明是否阻断

优先询问会改变目标、主流程或验收口径的阻断问题。非阻断问题可以记录默认假设，但必须明确标记为假设并让用户确认。

## 规格阶段

关键问题得到回答后，生成规格骨架：

- 目标
- 非目标
- 用户场景
- 限制
- 验收条件，使用 AC-001、AC-002 等稳定 ID
- 未决问题
- 计划准备状态

验收条件必须描述可观察结果，避免只写“完成开发”“优化体验”等无法验证的动作。每条验收条件只表达一个主要结果，后续计划和验证证据应显式引用其 ID。

## 进入计划的条件

只有目标、至少一个用户场景和至少一个可验证验收条件已明确，且没有阻断问题时，才标记“可以进入计划”。否则输出当前规格骨架和最少量的下一组澄清问题，不假装需求已经完整。

## 写入与授权边界

- 探索和澄清默认在当前对话中进行，不因存在活动任务而自动修改过程文档。
- 用户明确要求保存规格时，只写入用户授权的位置；当前版本没有专用 CLI 持久化入口时，不虚构命令。
- 澄清完成不代表授权修改代码、创建计划、归档、提交或发布。
- 需求存在多个方向且选择会实质改变范围时，停止实施并请用户决定。

## 输出要求

先给当前结论，再列出已确认内容和待澄清问题。问题数量以解除当前阻断为限，不一次堆叠低价值问卷。规格完成后建议进行只读跨产物语义分析或进入计划管理，但不自动启动后续动作。`
};
