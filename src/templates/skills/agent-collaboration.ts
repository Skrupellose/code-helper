import type { SkillTemplate } from "./types.js";

export const agentCollaborationSkillTemplate: SkillTemplate = {
  name: "code-helper-agent-collaboration",
  directoryName: "code-helper-agent-collaboration",
  fileName: "agent-collaboration.SKILL.md",
  content: `---
name: code-helper-agent-collaboration
description: 当开始新需求、迁移、重构、反馈修复、多人或多 agent 协作任务时必须使用。该 skill 使用 T0-T3 风险与复杂度分级决定由主会话直办、建议分发或强制实现与复核隔离，同时保留主会话审阅责任、执行范围隔离和跨平台降级规则。
---

# Code Helper Agent 协作

## 目标

在同一项目中统一主会话与子代理的职责边界，让分发成本与任务风险相匹配，同时避免未经审阅的结论和范围外修改。

核心目标：

1. 主会话始终负责目标理解、范围控制、执行方式选择、结果审阅和最终结论。
2. T0/T1 允许主会话直办，T2 建议分发，T3 强制实现与复核隔离。
3. 主会话始终可以直接进行只读证据核验和非变更型验证。
4. 每次派发都限定写入范围和完成条件，避免扩大影响面。
5. 当前工具没有子代理能力时按任务等级降级，不让低风险任务无条件阻塞。

## 使用场景

当用户表达以下意图时，使用本 skill：

- 开始新需求、新功能、迁移、重构或反馈修复
- 需要读取大量代码或多份文档
- 需要修改文件、运行命令、执行测试或整理资料
- 多人或多 agent 交替推进同一项目
- 用户提醒“你不是独自工作”或要求按 Agent 协作规范执行
- 准备把任务拆给不同执行者并汇总结果

## 工作流

### 1. 主会话确认目标和边界

主会话先确认用户目标、必须遵守的入口规则、禁止触碰的目录、允许修改的文件范围、验证要求和交付格式。

如果任务涉及项目规则，还要先读取 AGENTS.md、CLAUDE.md 或项目中已声明的专题规则索引。

主会话始终可以自行执行以下审阅动作，不需要为了形式上的角色隔离额外派发：

- 只读证据核验
- 查看工作树、提交或 PR 的 diff
- 搜索定义、引用和调用方
- 运行不会修改项目状态的静态检查和定向测试
- 读取子代理返回中引用的文件、日志和验证结果

### 2. 按 T0-T3 判断执行方式

| 等级 | 判定 | 执行方式 |
|---|---|---|
| T0 | 精确只读查询、状态检查、单一证据核对或不产生项目变更的微型操作 | 主会话直接执行 |
| T1 | 边界清晰、局部可逆、无外部副作用且可用定向验证覆盖的任务 | 主会话可直办，也可在上下文较大时分发 |
| T2 | 跨模块调用链、根因不确定、多文件协作或具有明确独立工作包的任务 | 建议分发；主会话直办时必须收紧范围并提高复核强度 |
| T3 | 会改变或绕过安全/权限门禁、处理真实敏感用户数据、执行不可逆迁移、生产发布、外部写操作或其它高影响副作用的任务 | 必须让实现与复核相互隔离；缺少隔离能力时不得实施，普通用户确认不能替代独立隔离 |

判断等级时同时考虑影响面、可逆性、外部副作用、根因确定性、验证难度和工作包独立性。文件数量只能作为辅助信号，不能单独决定任务等级。执行中发现范围或风险扩大时，立即重新分级。

领域名称或代码中出现“安全”“权限”“用户数据”“文件下载”等词，不能单独判定为 T3。只有本轮变更实际触碰门禁强度、敏感数据、不可逆状态或生产外部副作用时才进入 T3；纯逻辑安全加固、测试补充或受控本地文件处理仍按真实影响面判定。

### 3. 选择单代理、串行或并行

- T0/T1 优先避免不必要的派发；主会话直办时仍要声明写入范围、保留用户已有改动并检查实际 diff。
- 顺序强依赖链优先由同一执行代理负责到底，避免每个步骤重复加载上下文。
- 补充任务优先复用原执行代理；只有原代理不可用、发生范围冲突或需要真正独立复核时才更换。
- 只有子任务互不依赖、允许写入范围不重叠、共享基线稳定且合并成本低于并行收益时才并行。
- 同一文件、同一状态机、根因尚未确定或 review → 修复 → 复审链路优先串行。
- 即使代码写入范围不重叠，只要验证命令会共同写入 \`dist\`、\`coverage\`、缓存、快照、依赖目录或其它共享产物，也必须串行执行；不能把“测试不同文件”误判为可安全并行。

单个逻辑交付点默认使用以下协作预算：

- T0/T1 默认不派子代理；只有上下文过大且存在明确独立工作包时，T1 才使用一个执行子代理。
- T2 默认使用一个执行子代理；只有两个工作包真正独立且并行收益明确时，才增加到两个。
- T3 默认使用一个实现子代理和一个独立复核子代理；不得为了增加“复核覆盖”无条件启动三路或更多同类复核。
- 单节点默认最多触发 4 个子代理任务；每次新派发或重新触发都计入预算，复用同一 Agent 继续修复也不应演变成无限任务链。
- 预算是上限，不是必须用满的配额。预算将耗尽时优先收紧范围、串行复用原 Agent 或由主会话做只读核验。
- T3 的独立复核是强制门禁。若未关闭的 P0/P1 使任务必须突破默认预算，不得跳过复核；应停止扩张并请用户决定继续当前阻断项还是调整范围。

### 4. 分配验证责任并复用证据

主会话在派发 T2/T3 任务或存在重复验证风险时，先建立最小验证矩阵。每个验证项至少说明：

- 负责角色：执行子代理、主会话或独立复核者
- 执行模式：必须执行、复用回执或风险触发时复跑
- 命令、工作目录和覆盖的完成条件
- 会写入的共享资源，例如 \`dist\`、\`coverage\`、缓存、快照或依赖目录

同一基线、同一工作树状态和同一验证命令默认只执行一次。执行子代理已经给出可核验回执时，主会话先复用证据，不机械重复运行；只有回执缺失或失败、基线已经变化、结果疑似不稳定、验证不覆盖当前风险，或用户明确要求时才复跑受影响部分。

- T1：执行者运行定向验证，主会话核对回执和实际 diff。
- T2：执行者运行定向验证，主会话复用回执并审阅调用链；只有风险触发时才补跑关键子集。
- T3：实现者和独立复核者分别验证关键成功与失败路径，这是有意的隔离验证；不要求双方机械重复全部测试套件。
- \`test\`、\`check\`、\`build\`、\`pack\` 等全量命令在一个逻辑交付点内分别指定唯一默认负责人；共享副作用资源冲突的命令必须串行。

### 5. 使用最小派发提示

派发任务只要求以下最小必填语义：

1. 任务 ID，用于后续状态更新、取消和结果去重。
2. 执行角色与转派边界：必须可靠表明接收者是执行子代理，并禁止自行二次转派。该语义优先由工具提供的父任务、调用关系或角色元数据承载；只有元数据不可用或不能可靠传递时，派发提示才必须明确“你现在是执行子代理”。
3. 任务目标和完成条件。
4. 允许写入的文件或逻辑范围；纯只读任务明确不允许修改。
5. 本任务真正相关的关键约束。
6. 返回内容：实际改动、验证回执、风险和未完成项。

以下字段按风险和任务特征补充，不要求每次机械重复：

- 必读规则或上下文：上下文不能安全继承，或遗漏会改变结论时补充。
- 禁止触碰的目录、文件和命令：存在具体越界风险时补充。
- 基线身份：T2/T3 或共享工作树存在未提交改动时，记录起始 commit、允许存在的脏工作树摘要或等价快照。
- 完整验证矩阵、共享副作用资源和交付格式：T2/T3 或需要结构化交接时补充。

不要派发“看看哪里有问题”或“顺手修一下”等宽泛任务。任务必须收窄到可审阅、可验证的逻辑边界。

### 6. 执行子代理处理派发任务

当前会话优先使用工具提供的父任务、调用关系或角色元数据识别自己是否为执行子代理。只有元数据不可用或不能可靠传递时，才以任务说明中的“你现在是执行子代理”等明确派发上下文作为兼容回退。

执行子代理必须：

- 按派发任务限定的范围直接执行读取、修改、验证或文档产出
- 不再套用主会话的分发职责，不要求再次转派，不因为自己没有子代理能力而停止
- 只在派发范围内行动，不扩大修改范围，不回滚他人改动
- 遇到缺少工具、权限、文件或上下文时，说明具体阻塞和已完成范围
- 返回任务 ID、状态、起始基线、改动文件或等价 diff 身份、风险和未完成项，供主会话审阅
- 对自己负责的验证项返回验证回执：精确命令、工作目录、退出码、结果摘要、共享副作用资源和未验证范围

默认委派深度为 1，只有主会话负责派发。执行子代理不得自行二次转派，也不得把自己重新定义为主会话；只有收到用户直接需求且没有上游派发语境时，才按主会话流程判断执行方式。

### 7. 使用统一通信生命周期

每个已派发任务使用以下状态：\`accepted\`、\`running\`、\`waiting_input\`、\`blocked\`、\`completed\`、\`cancelled\`、\`superseded\`。只在状态变化、出现阻塞或形成可交接结果时发送更新，避免用高频无信息消息打断主会话。

状态更新至少包含任务 ID、当前状态、已完成范围、阻塞或下一步。主会话遵守以下规则：

- 等待或调用超时只表示本次等待结束，不等于子代理失败；重试或重新派发前，先查询任务状态，并检查实际工作树、产物和已有消息。
- 用户改变范围或主会话决定替换任务时，明确发送 \`cancelled\` 或 \`superseded\`；被替代任务的迟到结果不得直接进入最终结论。
- 补充修复优先复用原任务 ID 和原执行代理，只传递增量上下文；确需创建替代任务时使用新任务 ID，并引用被替代任务。
- 未确认旧任务状态前，不创建相同写入范围的重复执行任务。

### 8. 主会话按风险审阅

子代理返回后，主会话必须审阅：

- 是否只处理了派发范围内的任务
- 是否存在范围外改动
- 变更是否满足用户目标
- 验证命令、测试结论或未验证原因是否充分
- 风险、阻塞和后续事项是否说明清楚
- 是否需要继续派发补充任务

审阅强度与风险匹配：

- T0：核对关键证据和来源。
- T1：检查实际 diff、范围外改动和定向验证。
- T2：完整审阅调用链、实际变更、验证回执和剩余风险，必要时安排独立复核。
- T3：必须由未实施该变更的角色独立复核，复核者要读取实际 diff 并验证关键成功与失败路径。

主会话不能直接转述未经审阅的子代理结论。发现验证不足、范围扩大或结论不清时，补充任务优先交回原执行代理；需要独立复核时才使用不同代理。

主会话接收结果时必须核对任务 ID、起始基线和当前 diff 身份。工作树或基线在验证后发生变化时，只把受影响的回执标记为过期，并补跑对应验证；不得因为局部变化无条件重跑全部命令。

默认只进行一轮独立复审。修复后可以由原复核者核对原 finding 及其直接回归，但不得把“逐项复审”重新扩展成无边界全量审计。只有出现新的 P0/安全证据、实现范围发生实质扩大或用户明确要求时，才启动额外完整审查。

实现开始前必须固定当前节点的原始完成定义。审查发现的问题按“当前阻断 / 当前非阻断 / 后续优化”分类：只有直接阻止原始完成定义、造成明确安全问题或重要回归的当前阻断项自动进入本节点修复；其它问题默认记录到后续优化。原始完成定义满足且当前阻断项关闭后，必须停止自动扩修并进入结果汇报。

### 9. 同步状态并最终汇报

主会话只汇总已审阅的结果，并负责：

- 同步必要的 status-doc 或 result-doc
- 如果任务通过直接执行完成、没有后续阶段且具有复盘价值，由掌握本轮上下文的主会话生成独立完成记录，不派子代理倒写计划
- 判断是否需要询问更新长期记忆
- 判断是否需要询问归档
- 向用户说明改动文件、验证结论、风险和建议下一步

在节点开始、完成定义发生变化、关键实现完成、复审结束和最终回复前，主会话必须使用同一份紧凑进度摘要；普通搜索、单次命令和微型补丁不单独播报。摘要固定包含：

- 当前节点
- 完成定义进度，例如 3/5
- 已经完成
- 当前工作
- 剩余门禁
- 后续优化
- 下一步
- Agent 使用，例如 2/4；存在独立复审时同时标明复审轮次，例如 1/1

如果用户明确禁止更新过程文档、归档、运行同步命令或提交代码，必须遵守用户约束。

完成检查按可独立验收的逻辑交付点触发。搜索、读取、单次命令、单个补丁等微型步骤不单独触发；准备最终回复、提交前检查或切换任务时仍执行适用的完成检查。

直接执行任务如果仍有后续阶段、阻塞或跨会话恢复需求，必须升级为计划跟踪并创建 plan/status/result；只有任务已经完成且具有复盘价值时，才使用 \`code-helper-completion-record\` 生成独立终态记录。完成记录不得反向触发计划文档补齐。

## 无子代理能力时

如果当前 agent 工具没有可调用的子代理能力，按等级降级：

- T0/T1：主会话按原范围直接执行，不需要额外等待确认。
- T2：主会话可把任务收紧为单一工作包后执行，并明确缺少独立执行隔离、提高复核强度。
- T3：只继续只读调查、风险分析和方案整理。用户确认不能授权单会话绕过独立隔离，只能确认缩小或降级任务范围、改为只读方案、等待具备隔离能力，或明确决定修改项目的安全门禁规则。

不得把同一 agent 的自我复核描述成独立复核。本节只约束主会话；已经被主会话明确派发的执行子代理不应使用本节停止执行。

## 平台能力与兼容回退

协作前按当前实际工具判断能力，不根据 Codex、Claude Code、GitHub Copilot、Grok Build 等平台名称或 skills 注册目录猜测。能力画像至少包含：子代理、并行执行、角色元数据、会话复用、状态查询和中断。

- 没有子代理能力：按“无子代理能力时”降级。
- 没有并行能力：保持任务串行，不模拟并行。
- 没有可靠角色元数据：在派发提示中明确执行身份和禁止二次转派。
- 没有会话复用能力：新任务必须携带原任务 ID、基线身份、验证回执和未完成项。
- 没有状态查询能力：以已收到消息和工作树证据判断，等待超时不能当作失败。
- 没有中断能力：把旧任务标记为 \`superseded\` 并忽略其迟到结果，不假设任务已经停止。

能力不足只能触发对应回退，不能放宽 T3 独立隔离、写入范围、验证责任或结果审阅门禁。

## 边界规则

- 主会话对范围、执行方式、审阅和最终结论负责，T0/T1 直办不降低范围控制要求。
- 执行子代理必须直接完成派发范围内的任务，不得自行二次转派、扩大范围或触碰明确禁止的内容。
- 派发任务必须限定目标、写入范围、关键约束、完成条件和返回内容。
- 子代理结果必须按风险审阅后才能用于最终结论，T3 的实现与复核必须隔离。
- 单节点默认最多触发 4 个子代理任务、进行一轮独立复审；T3 未关闭 P0/P1 时不得用预算上限绕过强制复核。
- 原始完成定义满足且当前阻断项关闭后停止自动扩修；当前非阻断项和后续优化不得无限延长当前节点。
- 关键里程碑必须输出统一进度摘要，微型步骤不制造流水式播报。
- 无子代理能力时按 T0-T3 分级降级，不让低风险任务硬阻塞；T3 的普通用户确认不能替代独立隔离。
- 不回滚他人改动；如果发现无关改动，保留并在必要时说明。
- 用户明确给出的本轮约束优先，例如不运行同步命令、不改生成产物、不提交代码。`
};
