import type { CodeHelperConfig } from "../types.js";

/**
 * 返回内置专题规则模板。
 * 模板文本放在代码中，确保 npm 包无需额外复制资源也能完成初始化。
 */
export function getRuleTemplates(config: CodeHelperConfig): Array<{ fileName: string; content: string }> {
  const entryFiles = [
    config.entryFiles.agents ? "`AGENTS.md`" : undefined,
    config.entryFiles.claude ? "`CLAUDE.md`" : undefined,
    config.entryFiles.copilot ? "`.github/copilot-instructions.md`" : undefined
  ].filter((value): value is string => value !== undefined);

  return [
    {
      fileName: "项目记忆规则优化.md",
      content: `# 项目记忆规则优化

## 功能描述

维护项目级 agent 记忆，把项目规则组织成“轻量入口文档 + 专题规则文档”的结构，避免入口文件膨胀、规则重复和短期任务状态污染长期记忆。

## 调用时机

- 用户要求更新记忆、优化记忆、整理 AGENTS.md、CLAUDE.md 或 Copilot 入口
- 用户要求同步 AGENTS.md、CLAUDE.md 和 Copilot 入口
- 用户要求沉淀规则、把当前变更写入记忆、拆分项目规则文档
- agent 识别到功能变更、项目结构变化、测试策略变化、发布流程变化或稳定协作规则后
- 入口文档变长、重复或混入短期任务状态时

## 调用入口文件

${entryFiles.map((file) => `- ${file}`).join("\n")}

## 规则

1. 先判断任务类型：入口文档整理、AGENTS.md / CLAUDE.md / Copilot 入口同步、专题规则拆分、根据当前变更更新记忆、新增专题规则、修改已有专题规则、仅输出草案。
2. 写入前先读取项目根目录下已存在的 AGENTS.md、CLAUDE.md、.github/copilot-instructions.md 和 \`${config.directories.userRules}/\`，不要假设文件或目录存在。
3. 入口文档中的 \`<!-- code-helper:start -->\` 到 \`<!-- code-helper:end -->\` 是 code-helper 自动维护的受控区块；整理或优化 AGENTS.md、CLAUDE.md、.github/copilot-instructions.md 时，绝对不能改动该区块。
4. 自定义规则应写在 code-helper 受控区块外；长期规则写入 \`${config.directories.userRules}/\`；所有入口文档整理只允许作用于 code-helper 受控区块之外的内容。
5. 需要改变受控区块内容时，应修改 code-helper 模板源后运行 \`init\`、\`update\` 或 \`sync-local\`，不要直接编辑生成后的受控区块。
6. 若多个入口文件同时存在，同时识别并保持专题规则索引、文档维护规则和规则路径一致。
7. 若只存在一个入口文件，优先优化已有入口文件；只有用户明确要求多入口同步时，才创建缺失入口文件。
8. 若入口文件都不存在，先输出入口文档草案；只有用户明确要求写入时，默认创建 AGENTS.md。
9. 入口文档只保留项目概览、核心规则、常用命令、专题规则索引和文档维护规则。
10. 具体规则默认写入 \`${config.directories.userRules}/\`，不要把完整规范塞进入口文档。
11. 专题规则文档必须包含“功能描述 / 调用时机 / 调用入口文件 / 规则”四个小节。
12. 当 agent 判断可能需要更新记忆，或用户明确要求更新记忆时，先查看当前变更范围，再只更新相关专题文档。
13. 如新增主题不存在，新增对应专题文档；如入口索引缺失，再更新已存在的入口文件。
14. 不整份覆盖所有文档，不重复创建相似专题，不把一次性任务状态、临时调试过程、完整命令输出或短期计划写进长期记忆。
15. 新功能、小节点或重构形成稳定规则后，agent 必须主动询问用户是否更新记忆；用户确认前不自动写入长期记忆。

## 输出格式

仅输出草案时使用“记忆优化草案 / 入口文档调整 / 专题文档调整 / 定向更新规则 / 待确认问题”。

写入文件后使用“已完成记忆优化 / 校验”，列出更新的入口文件、专题规则文件和校验结果。

## 校验

1. AGENTS.md、CLAUDE.md 和 .github/copilot-instructions.md 如果存在，仍应保持轻量入口定位。
2. code-helper 受控区块必须逐字保持工具生成内容，不得被记忆优化流程增删、改写、重排或格式化。
3. 多个入口文件同时存在时，专题规则索引必须一致指向 \`${config.directories.userRules}/\`。
4. 每份专题规则文档都必须包含四个固定小节。
5. 专题文档路径必须被现有入口文件正确引用。
6. 不得误把当前调试状态、临时实现方案或一次性任务流水写入长期记忆。`
    },
    {
      fileName: "项目计划管理规范.md",
      content: `# 项目计划管理规范

## 功能描述

把完整需求文档拆成可推进、可记录阻塞、可恢复上下文、便于后续复查的执行计划。目标不是只写说明文档，而是生成 agent 和开发者可以持续使用的计划、状态和结果记录体系。

## 调用时机

- 用户提供完整需求文档并要求拆分计划
- 项目迁移、框架升级、多阶段功能建设、平台能力建设、数据任务、工具链建设或技术债清理
- 多模块、多系统、多角色、多数据流或跨端交付存在明显依赖关系的任务
- 需要长期推进、多人协作或中途暂停再恢复的任务

## 调用入口文件

${entryFiles.map((file) => `- ${file}`).join("\n")}

## 规则

1. 所有计划、结果、状态和测试文档必须使用中文命名与中文总结。
2. 计划文档写入 \`${config.directories.planDoc}/<中文功能名>.md\`。
3. 执行记录写入 \`${config.directories.resultDoc}/<中文功能名>/实施记录.md\`。
4. 当前状态写入 \`${config.directories.statusDoc}/<中文功能名>-状态.md\`。
5. 需要人工验收时，手工测试或验收文档写入 \`${config.directories.resultDoc}/<中文功能名>/手工测试.md\`。
6. status-doc 必须包含“当前执行节点”，写清当前子计划、状态、执行目标、进入条件、完成定义和验证方式。
7. status-doc 必须包含“子计划队列”，按顺序列出基础能力、核心实现、集成验收和完成整理等后续节点。
8. agent 每次继续任务时先读 status-doc，只推进当前执行节点；完成后同步更新 result-doc、plan-doc 和 status-doc 的下一个执行节点。
9. 最终计划必须同时具备总纲、分层顺序、模块或能力拆分、依赖与集成计划、验收标准、状态记录、阻塞点、后续检查点和下一步建议。
10. 生成顺序必须是：目标与约束、P0/P1 总纲、依赖顺序、目录或模块策略、基础阶段计划、核心实现计划、集成验收计划、执行计划。
11. 不要一开始直接写细表；先给总纲和阶段边界，再逐步细化。
12. 必须按依赖链路重排顺序，不按需求文档顺序、界面直觉或个人偏好排序。
13. 推荐依赖顺序：目标和范围、现状和影响面、基础结构、数据模型或接口契约、核心业务规则、关键模块或服务、集成链路、验证方案、发布和交接整理。
14. 不预设任务一定是前端页面或组件；应按实际需求拆成功能模块、领域能力、接口、命令、任务、作业、页面、组件、数据流或服务单元。
15. 集成和验收计划必须包含前置依赖、执行任务、验证方式、验收标准、完成定义和后续检查点。
16. 所有总览表和明细表统一使用“子任务 / 已完成 / 未完成 / 备注”四列。
17. 备注第一句必须写 \`状态：未开始\`、\`状态：进行中\`、\`状态：部分完成\`、\`状态：被阻塞\` 或 \`状态：已完成\`。
18. 备注必须说明阻塞点、依赖后续流程、后续检查点，以及是否允许跳过继续推进。
19. 文档前部必须包含“下一步建议”，明确现在先做什么、下一步接什么、第一个核心模块或能力何时开始、第一个集成或验收环节何时开始、当前默认执行主线是什么。
20. 验证计划必须按任务类型选择；纯逻辑、数据转换、接口契约、CLI 命令、权限规则和后端服务优先规划可自动执行的单元测试或集成测试。
21. 页面、可视化、组件交互、视觉和真实浏览器链路只生成严格手工测试文档；工具自身只执行纯逻辑测试。
22. 文档只有满足可直接推进、可记录阻塞、可恢复上下文、便于后续复查时，才算完整。`
    },
    {
      fileName: "执行结果总结规范.md",
      content: `# 执行结果总结规范

## 功能描述

规范小节点完成后的实施记录，保证后续 agent 能恢复上下文并判断风险。

## 调用时机

- 完成一个可交接的小粒度任务后
- 阶段内出现重要阻塞、回滚点或验证结论后
- 准备更新当前状态记录前

## 调用入口文件

${entryFiles.map((file) => `- ${file}`).join("\n")}

## 规则

1. 结果文档默认写入 \`${config.directories.resultDoc}/<中文功能名>/实施记录.md\`。
2. 文档必须包含背景、实现、验证、风险与后续。
3. 验证只记录关键命令和结论，不粘贴冗长输出。
4. 如果页面需要测试，写入手工测试文档，不要求 agent 执行浏览器自动化。
5. 影响下一步决策的结论同步到 \`${config.directories.statusDoc}/\`，细节留在 result-doc。`
    },
    {
      fileName: "测试策略规范.md",
      content: `# 测试策略规范

## 功能描述

约束 agent 的测试边界，减少慢且不稳定的页面自动化，把页面验收交给严格手工测试文档。

## 调用时机

- 任务涉及页面、组件交互、视觉验收或浏览器真实链路
- 任务涉及纯函数、数据转换、接口封装或可稳定自动化的逻辑
- 阶段结束需要说明测试范围时

## 调用入口文件

${entryFiles.map((file) => `- ${file}`).join("\n")}

## 规则

1. 页面相关测试全部生成严格手工测试文档，由用户执行。
2. 工具自己只执行纯逻辑测试，例如函数单元测试、数据转换测试、非浏览器集成测试。
3. 不默认引入 Playwright、截图对比或浏览器自动化作为页面验收手段。
4. 手工测试文档必须包含测试环境、前置数据、操作步骤、预期结果、回归范围和阻塞记录。
5. 如果用户明确要求执行页面自动化，先提示这会偏离默认策略，再按用户明确指令处理。`
    },
    {
      fileName: "文档归档规范.md",
      content: `# 文档归档规范

## 功能描述

管理已完成或已结束功能的计划、结果和状态文档，避免多个功能长期堆在当前工作区里，影响 agent 判断下一步任务。

## 调用时机

- 一个功能、阶段或反馈修复已经完成并完成必要总结后
- 用户要求归档某个功能文档
- 用户手动把文档移动到 archive 目录后，需要识别该任务已经结束
- agent 需要判断当前项目还有哪些 active 任务、哪些 archived 任务

## 调用入口文件

${entryFiles.map((file) => `- ${file}`).join("\n")}

## 规则

1. 活动任务文档保留在当前工作区：\`${config.directories.planDoc}/\`、\`${config.directories.resultDoc}/\`、\`${config.directories.statusDoc}/\`。
2. 已结束任务文档放入归档目录：\`${config.directories.planDoc}/archive/\`、\`${config.directories.resultDoc}/archive/\`、\`${config.directories.statusDoc}/archive/\`。
3. 执行 \`npx @skrupellose/code-helper archive <中文功能名>\` 时，将 \`${config.directories.planDoc}/<中文功能名>.md\`、\`${config.directories.resultDoc}/<中文功能名>/\`、\`${config.directories.statusDoc}/<中文功能名>-状态.md\` 移入对应 archive 目录。
4. 归档不覆盖已有目标；如果 archive 中已经存在同名文档，视为用户已经手动归档。
5. 只要任务文档只存在于 archive 目录中，就应识别为已结束任务。
6. 如果同一中文功能名同时存在 active 文档和 archived 文档，状态为 mixed，必须人工确认是否有遗漏文档需要继续归档。
7. 新功能开始时不要复用已归档中文功能名；需要返工时，新建后续中文功能名或明确从 archive 恢复。
8. status-doc 是当前状态记录，归档后不再作为当前任务入口。
9. 初始化预建的空 archive 目录不代表存在归档任务；只有实际 archived 或 mixed 任务文档才参与生命周期判断。
10. 归档前必须检查实施记录和状态记录。只有页面、可视化、浏览器真实链路、人工业务验收任务，或结果目录已经存在 \`手工测试.md\` 时，才把手工测试结论作为归档条件；纯逻辑任务以自动化验证为准。`
    },
    {
      fileName: "功能完成检查规范.md",
      content: `# 功能完成检查规范

## 功能描述

约束 agent 在完成实现、文档或功能变更节点后准备最终回复，或切换任务前，主动判断当前功能是否真正完成，并据此更新过程文档、询问是否更新长期记忆、询问是否归档或继续当前节点。普通问答、只读 review 和未产生变更的最终回复不触发。

## 调用时机

- 完成一个小节点、阶段任务、反馈修复或功能开发后
- 修改了功能行为、项目结构、配置、测试策略、发布流程、CLI 交互、初始化逻辑或协作文档生成逻辑后
- 完成实现、文档或功能变更节点后准备最终回复，或提交代码、归档文档、切换到下一个任务前
- 用户表达“这个逻辑以后都这样”“沉淀为规则”“功能完成了”“继续下一个任务”等意图时
- agent hook 或用户手动执行 \`npx @skrupellose/code-helper finish\` 时

## 调用入口文件

${entryFiles.map((file) => `- ${file}`).join("\n")}

## 规则

1. 完成检查不是只在固定节点触发；只要 agent 识别到功能变更、项目改变或稳定规则变化，就必须主动判断是否需要进入完成检查。
2. 每次完成检查先读取 \`${config.directories.statusDoc}/<中文功能名>-状态.md\`，再读取对应 plan-doc 和 result-doc。
3. status-doc 是当前执行入口，必须能看出当前执行节点、子计划队列、完成定义、验证方式和下一步。
4. 当前小节点未完成时，不询问归档，不引导新任务；继续当前节点，或在 status-doc 记录阻塞原因和恢复条件。
5. 当前小节点完成但功能整体未完成时，更新 \`${config.directories.resultDoc}/<中文功能名>/实施记录.md\`、plan-doc 对应任务状态和 status-doc 的下一个执行节点。
6. 小节点完成后必须主动判断是否形成长期规则；如果形成稳定协作规范、项目约束、测试策略、发布流程或工具使用偏好，询问用户是否更新项目记忆。
7. 如果只是一次性实现细节、临时失败过程或短期调试状态，不写入长期记忆。
8. 功能整体完成时，先确认过程文档和验证结论已更新，再询问用户是否更新长期记忆、是否归档文档、是否选择下一个活动任务。
9. 更新长期记忆和归档文档都需要用户确认，不自动执行。
10. agent hook 只做提醒和检查，不替代 agent 判断；Git hook 只做提交前兜底。
11. 推荐运行 \`npx @skrupellose/code-helper finish <中文功能名> --check-only\` 作为收尾检查，不让检查命令直接修改项目文件。
12. 如果 \`finish\` 输出仍有未开始、进行中、部分完成或被阻塞节点，agent 必须继续当前功能，而不是切换新任务。
13. mixed 任务必须优先处理 active/archive 冲突；没有 active 但仍有 mixed 时，不得只报告“没有活动任务”。
14. 没有 active 且没有 mixed 任务时，不把空 archive 目录当成任务，也不为普通最终回复虚构完成检查；用户明确查看 archived 任务时，只说明它已结束。
15. 如果任务只存在于 archive 目录中，\`finish\` 应识别为已归档、已结束状态，不再要求补 active 路径下的 plan/result/status 文档，也不因归档正文中的历史“下一步”重复询问记忆或归档。
16. 任务文档已经明确记录“长期记忆已更新、已沉淀或无需更新”且不存在“尚未、仍需、下一步更新”等当前待办时，不重复生成长期记忆确认；正负表达共存时以待办为准。
17. \`finish\` 输出的“必须确认事项”是强制收尾清单；最终回复前必须逐项处理或明确询问用户，不能只总结已完成内容。
18. 如果“必须确认事项”包含更新长期记忆、归档文档或选择下一个任务，agent 必须在最终回复中明确提出对应问题；用户确认前不得自动执行。`
    },
    {
      fileName: "Git提交信息格式规范.md",
      content: `# Git 提交信息格式规范

## 功能描述

统一项目 Git 提交信息格式，使提交历史便于阅读、检索、审查、回滚和版本发布，并为后续接入提交校验或自动生成变更日志保留兼容基础。

## 调用时机

- 准备执行 git commit 前
- 为当前改动拟定或审查提交信息时
- 拆分、合并或整理多个提交时
- 提交版本号变更或准备版本发布时
- 创建 breaking change、关联 issue 或回滚提交时
- 审查 PR 的提交历史是否清晰时

## 调用入口文件

${entryFiles.map((file) => `- ${file}`).join("\n")}

## 规则

### 1. 基本格式

普通提交统一使用：

\`<type>(<scope>): <subject>\`

Breaking change 提交统一使用：

\`<type>(<scope>)!: <subject>\`

1. type 必填，使用小写英文。
2. scope 必填，使用小写英文或 kebab-case，表示主要影响领域。
3. 普通提交不能带感叹号；breaking change 必须在 type 和 scope 后增加感叹号。
4. subject 必填，默认使用简洁中文；命令、API、包名和平台名保留原始英文。
5. 冒号后必须有一个半角空格。
6. type 和 scope 使用英文，subject 和 body 默认使用中文。

### 2. Type 列表

| Type | 使用场景 |
|---|---|
| feat | 新增用户可感知的功能或能力 |
| fix | 修复缺陷、回归或错误行为 |
| docs | 只修改 README、指南或规则文档 |
| refactor | 不改变外部行为的代码重构 |
| test | 新增、修复或调整测试 |
| perf | 性能优化 |
| build | 构建脚本、打包配置或依赖构建方式变更 |
| ci | 持续集成或发布流水线配置变更 |
| chore | 版本号、依赖维护、仓库配置等非功能变更 |
| style | 仅格式、缩进或无行为影响的代码样式调整 |
| revert | 手工组织的回滚提交 |

版本发布不新增 release type，统一使用 \`chore(release)\`。

### 3. Scope 规则

1. scope 必填，必须对应本次提交的主要影响领域。
2. 推荐 scope：cli、init、skills、hooks、checks、templates、docs、readme、ci、release、deps。
3. scope 使用小写英文或 kebab-case，不使用文件路径、中文、逗号拼接或临时任务名。
4. 跨多个领域时选择最主要且可稳定识别的领域；如果无法确定，应先拆分提交，不能省略 scope。
5. 新 scope 应对应稳定模块或长期领域，不能为单次提交随意造词。

### 4. Subject 规则

1. 默认使用中文，采用“新增、修复、优化、调整、重构、更新、移除、发布”等直接表达。
2. 一句话说明结果，避免“处理问题”“更新代码”“review fix”等模糊描述。
3. 不以句号、感叹号或省略号结尾。
4. 建议不超过 50 个字符，最多不超过 72 个字符。
5. 不写文件清单，不重复堆叠无意义信息。
6. 一个提交只描述一个可独立理解、独立验证和独立回滚的逻辑变更。

示例：

- \`feat(skills): 支持 Grok Build 原生 Skills\`
- \`fix(cli): 修复取消菜单后进程未退出\`
- \`docs(readme): 补充跨平台安装说明\`

### 5. Body 规则

简单且可从 subject 完整理解的提交可以省略 body。以下情况必须写 body：

- 跨模块修改
- 修复原因不直观
- 存在兼容性或迁移边界
- 修改发布、回滚、安全或跨平台逻辑
- breaking change
- subject 无法说明关键设计取舍

subject 与 body 之间空一行。body 优先说明为什么修改、采用什么方案、重要边界和验证方式；不要直接复制 git diff 文件列表，必要时使用简短项目符号。

### 6. Footer 与 Issue

任何 footer 前都必须与上一段空一行：有 body 时与 body 空一行，没有 body 时与 subject 空一行。关联 issue 时使用：

- \`Refs #123\`：仅关联
- \`Closes #123\`：合并后应关闭
- \`Fixes #123\`：明确修复该 issue

没有对应 issue 时不得编造编号。多人协作可保留 Git 标准 footer：\`Co-authored-by: Name <email>\`。

### 7. Breaking Change

破坏现有 CLI 参数、公共 API、配置结构、生成目录、持久化状态或兼容行为时，必须同时：

1. 在 type 和 scope 后增加感叹号。
2. 编写独立 body，说明变更原因和主要行为变化。
3. 在 footer 中增加 \`BREAKING CHANGE:\`。
4. 在 footer 说明受影响用户、迁移方式和兼容边界。

Breaking change 的 body 必填，\`BREAKING CHANGE:\` footer 不能替代 body。即使项目仍处于 0.x，也不能省略 breaking change 标识。

示例：

    feat(cli)!: 调整 init 的默认目标推断

    无明确入口的新项目不再自动选择 agent，避免静默安装非预期资产。

    BREAKING CHANGE: 新项目必须显式选择 agent 目标；自动化脚本应传入明确目标。

### 8. 版本发布提交

版本提交统一使用：

\`chore(release): 发布 <version>\`

版本提交原则上只包含 package.json、锁文件中的对应版本，以及明确属于该版本的发布说明或元数据。功能实现和缺陷修复应先作为独立提交完成，不与版本号提交混合。

body 可以记录关键验证命令和结论。发布 workflow 不自动修改版本号或创建 tag 时，提交信息不能暗示尚未完成的远端发布已经成功。

### 9. Merge 提交

1. 托管平台自动生成的 merge commit 可以保留平台默认格式。
2. 不手工创建无上下文的 Merge branch 提交。
3. 使用 squash merge 时，最终 subject 必须符合本规范并包含必填 scope。
4. PR 包含多个独立逻辑主题时，应在合并前整理提交，不依赖 merge message 掩盖混乱历史。

### 10. Revert 提交

优先使用 \`git revert\`，保留 Git 自动生成的 \`Revert "<原提交 subject>"\` 和 \`This reverts commit <sha>.\`，不要手工删除原提交 SHA。

若必须手工组织回滚提交，使用 \`revert(<scope>): 撤销<原变更摘要>\`，并在 footer 中记录 \`Reverts: <commit-sha>\`。

### 11. 提交拆分

1. 不把功能、无关修复、文档整理和版本发布塞入同一个提交。
2. 能够独立验证和独立回滚的变更，应拆为独立提交。
3. 测试与其直接验证的实现通常放在同一提交。
4. 纯文档补充可独立使用 docs type。
5. 当前工作树包含多个逻辑主题时，提交前必须按主题暂存和复核。

### 12. 自动校验边界

1. 项目未引入 commitlint、Husky 或 conventional-changelog 时，本规范作为人工和 agent 提交前检查依据，不假设自动校验已经存在。
2. 后续引入自动校验时，优先用 commitlint 校验 header 结构、type 和必填 scope。
3. 提交格式校验使用 commit-msg hook，不复用功能完成检查或其它 Git hook 职责。
4. CI 可以校验 PR 提交或 squash 标题，但不得自动重写用户提交。
5. 自动规则必须与本专题文档保持一致。

### 13. 反例

- \`更新代码\`：缺少 type 和 scope，无法判断改动目的。
- \`fix: 修复版本缓存\`：缺少必填 scope。
- \`feat(cli,skills): 增加功能。\`：scope 拼接多个领域，subject 以句号结尾。
- \`Release 0.1.8\`：版本提交格式不统一。
- \`feat(cli): 更新功能和修复问题\`：主题模糊且混合多个逻辑变更。`
    },
    {
      fileName: "Agent协作规范.md",
      content: `# Agent 协作规范

## 功能描述

统一多个 agent 在同一项目中的协作入口、状态记录和交接方式。

## 调用时机

- 新 agent 接手项目或恢复上下文
- 开始新任务、拆分计划、记录阶段结果或检查规则
- 完成小节点、识别到功能变更、准备最终回复或切换任务前
- 多人或多 agent 交替推进同一项目时

## 调用入口文件

${entryFiles.map((file) => `- ${file}`).join("\n")}

## 规则

1. 开始任务前先读入口文档和相关专题规则。
2. 大任务先生成计划文档，再进入实现。
3. 小节点完成后写结果总结，避免只把结论留在对话里。
4. 当前仍影响决策的信息写入 status-doc，历史细节写入 result-doc。
5. 不覆盖用户已有规则；确需重写时先说明变更范围和原因。
6. 主会话定位为协调者，只负责理解目标、拆分任务、分配子代理、审阅结果、同步过程文档和向用户汇报，不直接执行具体实现、调试、测试、文件修改或资料整理。
7. 任何需要读取大量代码、修改文件、运行命令、排查问题、生成文档、执行测试或整理数据的任务，都必须由子代理执行。
8. 派发子代理时，主会话必须写清任务目标、允许修改的范围、必须读取的规则、禁止触碰的内容、预期产物和验证方式，避免子代理扩大范围。
9. 子代理返回后，主会话必须审阅变更、验证结论、风险和未完成项；发现范围外改动、验证不足或结论不清时，继续派发补充任务或要求修正。
10. 主会话只汇总已审阅的子代理结果，并负责同步 status-doc、result-doc、长期记忆更新询问和最终回复。
11. 如果当前 agent 工具没有子代理能力，主会话必须先明确说明“当前工具不支持子代理执行”，列出原本应交给子代理的任务和由主会话执行的影响，等待用户确认后才能继续执行；用户未确认时不得擅自开始具体任务。
12. 如果当前会话收到主会话派发，且派发内容明确写出“你现在是执行子代理”或等效说明，则当前会话就是执行子代理，必须在授权范围内直接读取、修改、验证和汇报，不得再以“主会话必须派发子代理”为由停止执行或二次转派。
13. 执行子代理发现自身缺少必要工具、权限或上下文时，只说明具体阻塞和已完成范围；不要泛化为“当前工具不支持子代理执行”，除非它确实是主会话且正在尝试派发子代理。
14. 完成小节点、识别到功能变更、准备最终回复或切换任务前，必须执行功能完成检查，判断是否继续当前节点、更新过程文档、询问更新长期记忆、询问归档或选择下一任务。
15. agent 识别到稳定规则、项目结构变化、测试策略变化、发布流程变化或长期协作偏好时，应主动询问用户是否更新项目记忆；用户确认前不自动写入长期规则。
16. Agent hooks 只作为完成检查提醒和兜底，不替代 agent 自己判断；Git hooks 只做提交前检查。`
    }
  ];
}
