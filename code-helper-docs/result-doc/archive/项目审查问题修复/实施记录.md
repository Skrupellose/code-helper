# 项目审查问题修复实施记录

## 背景

本次根据两个子代理的 review 结论处理项目缺陷和优化点，覆盖 hooks 默认安装、计划文档产物、归档状态、完成检查、配置检查、发布构建和 README 说明。

## 实施总结

已完成全部 review 项的代码、测试和文档更新。未执行提交、推送或发布。下一步是由用户确认是否归档本任务文档，或继续安排后续发布。

## 实现

- 在 `src/hooks.ts` 和 `src/cli.ts` 中调整 hooks 目标解析，缺少 target 时不再默认处理全部 hooks。
- 在 `src/workflows.ts` 中调整计划产物，默认只创建计划文档、实施记录和状态记录，并优化中文文档名归一化以降低碰撞风险。
- 在 `src/archive.ts` 中增加 mixed 归档冲突预检查和显式 `resolveMixed` 处理。
- 在 `src/completion.ts` 中新增 archived 完成检查状态，并读取 archive 路径下的任务文档。
- 在 `src/checks.ts` 中让检查默认只读，并补充原始配置缺失功能开关检查。
- 在 `package.json` 中新增 `prepack` 构建保护。
- 更新 `README.md` 和相关单元测试，覆盖新行为。
- 按用户确认更新长期记忆，定向更新 `code-helper开发与发布规范.md`、`项目计划管理规范.md`、`文档归档规范.md` 和 `功能完成检查规范.md`。

## 验证

- `npm test`：通过，57 个测试全部通过。
- `npm run check`：通过，未发现协作文档结构问题。
- `npm pack --dry-run`：通过，`prepack` 已触发构建，dry-run 打包成功。
- `node dist/index.js finish --check-only`：通过，提示当前存在活动任务，需要指定任务做精确检查。
- `npm run check`（记忆更新后）：通过，未发现协作文档结构问题。

## 风险与后续

- `npx @skrupellose/code-helper finish --check-only` 在本地返回 `code-helper: command not found`，已改用 `node dist/index.js finish --check-only` 完成同等检查。
- `archive --resolve-mixed` 会清理 active 副本，只应在用户确认保留 archive 版本后使用。
- 本次形成的稳定行为规则已按用户确认写入长期项目记忆。
