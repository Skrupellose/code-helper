# 完成步骤防漏优化实施记录

## 背景

用户指出当前任务结束后没有触发归档校验和提问，要求先归档已完成任务，并进一步优化逻辑，确保不漏掉设定好的收尾步骤。

## 实施总结

已先归档 3 个已完成任务并提交；随后将完成检查的关键确认项显式化，新增 `requiredConfirmations` 和 CLI “必须确认事项”输出。本次优化已完成实现、规则同步和本地验证。

## 实现

- 执行并提交已完成任务归档：`项目审查问题修复`、`GitHubActions发布自动化`、`菜单优化`。
- 在 `src/completion.ts` 中新增 `requiredConfirmations`，集中生成必须询问或禁止执行的收尾事项。
- 在 `src/cli.ts` 中新增“必须确认事项”输出区块。
- 在 `tests/completion.test.js` 和 `tests/cli.test.js` 中补充防漏测试。
- 更新 `code-helper-docs/user-rules/功能完成检查规范.md` 和 `src/templates.ts`。
- 刷新项目级 skills，并同步 `.code-helper/skills/completion-review.SKILL.md` 模板源。

## 验证

- 已通过：`npm run build`
- 已通过：`npm test`
- 已通过：`npm run check`
- 已通过：`npm pack --dry-run`

## 风险与后续

- `requiredConfirmations` 只负责明确输出必须确认的问题，仍不自动执行归档、记忆更新、提交或发布。
- 后续如果新增收尾动作，必须同步更新 `requiredConfirmations`、CLI 输出和测试。
