# 完成步骤防漏优化状态

## 当前执行节点

| 字段 | 内容 |
|---|---|
| 当前子计划 | 最终完成检查 |
| 当前状态 | 已完成 |
| 执行目标 | 执行完成检查，确认最终回复前必须处理的确认事项 |
| 进入条件 | 代码、规则、模板、skills 和测试已更新 |
| 完成定义 | `npm run build`、`npm test`、`npm run check`、`npm pack --dry-run` 通过，完成检查输出已处理，并提交本次优化 |
| 验证方式 | 本地命令验证和 `finish 完成步骤防漏优化 --check-only` |

## 子计划队列

| 子任务 | 已完成 | 未完成 | 备注 |
| --- | --- | --- | --- |
| 归档已完成任务 | 三个历史完成任务已归档并提交 | 无 | 状态：已完成。后续检查点是归档后确认任务列表。 |
| 防漏逻辑实现 | `requiredConfirmations` 和 CLI 输出已实现 | 无 | 状态：已完成。后续检查点是新增收尾动作时同步该列表。 |
| 规则与模板同步 | 当前规则、模板源和项目级 skills 已同步 | 无 | 状态：已完成。后续检查点是模板变更后继续刷新 skills。 |
| 验证与提交 | `npm run build`、`npm test`、`npm run check`、`npm pack --dry-run` 已通过 | 提交本次优化 | 状态：已完成。完成检查后进入最终回复。 |

## 阻塞点

无。

## 后续检查点

- 最终回复前必须运行 `finish 完成步骤防漏优化 --check-only`。
- 如果输出“必须确认事项”，必须逐项向用户说明或询问。
- 本任务完成后应询问是否归档。
