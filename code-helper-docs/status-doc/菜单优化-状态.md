# 菜单优化状态

## 当前执行节点

- 子计划：完成整理
- 状态：已完成
- 执行目标：确认主菜单分组、命名、说明、布局清晰度、测试、README 和长期规则文档均已完成。
- 进入条件：CLI 行为、文档和测试已完成修改。
- 完成定义：验证命令全部通过，最终回复可说明修改文件、关键行为、验证结果和未解决问题。
- 验证方式：运行 `npm test`、`npm run check`、`npm pack --dry-run`。

## 子计划队列

| 子任务 | 已完成 | 未完成 | 备注 |
| --- | --- | --- | --- |
| 基础能力 | 读取项目规则和现有 init、plan、manual-test、finish、archive、tasks、skills、hooks、check、features 实现 | 无 | 状态：已完成。后续检查点是保持非交互子命令入口不倒退。 |
| 核心实现 | 主菜单分组、命名、说明、raw mode 固定说明列和数字兜底说明缩进行已完成 | 无 | 状态：已完成。后续检查点是新增菜单项时同步更新主菜单数据和测试。 |
| 集成验证 | 菜单构建测试、布局格式测试、目标解析测试、完成检查路径解析测试和三项验证命令已通过 | 无 | 状态：已完成。后续检查点是必要时补充真实交互手工验证。 |
| 完成整理 | README、长期规则和过程文档已更新 | 无 | 状态：已完成。可进入最终汇报。 |

## 阻塞点

无。

## 后续检查点

- 后续新增主菜单项时，必须同步评估分组、名称、简短说明、raw mode 选项、数字兜底菜单、说明对齐和测试。
- 后续新增 agent 工具时，必须同时评估 Skills 目录、Agent hook 支持情况、菜单选项、文本兜底解析和测试。
- 如需要发布 npm，必须由用户另行确认；本次不提交、不推送、不发布。
